if currentSituationType == c_aliens return "ALIEN ATTACK IMMINENT";
if currentSituationType == c_empty return "NO IMMIDIATE THREATS";
if currentSituationType == c_asteroids return "ASTEROID FIELD";
if currentSituationType == c_meteors return "METEOR SHOWER";
if currentSituationType == c_station return "SUPPLY STATION";
if currentSituationType == c_junk return "USEFUL DEBRIS DETECTED";
if currentSituationType == c_pirates return "SKULL PIRATES' SECTOR";
if currentSituationType == c_end return "HOME PLANET IN SIGHT";
if currentSituationType == c_warp return "ANOMALITY DETECTED";
